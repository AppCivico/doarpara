// Temporarily disabled to test CPU usage
// Event handlers and Date.now() calls add overhead that could contribute to CPU limit issues
export default defineEventHandler((event) => {
  // Middleware disabled for CPU testing
});
