# The use of quotation marks depends on environment setup
APP_TITLE='DoarPara'

BASE_URL=http://localhost:3000

CONTROL_PANEL_ORIGIN=http://localhost:3001

# required! `0` or `1`
IUGU_IS_TESTING=1

# miliseconds
CAMPAIGN_POOLING_INTERVAL=300000

MASKS_CREDIT_CARD_EXPIRATION_CSC='["###", "####"]'
MASKS_CREDIT_CARD_EXPIRATION_DATE_MASK='["##/##", "##/####"]'
MASKS_CREDIT_CARD_EXPIRATION_DATE_PLACEHOLDER='MM/YY ou MM/YYYY'
MASKS_CREDIT_CARD_NUMBER='["#### #### #### ####", "#### #### #### ##"]'
MASKS_NATURAL_PERSON_IDENTIFICATION='###.###.###-##'
MASKS_ZIP_CODE='#####-###'

NUXT_PUBLIC_GTAG_ID=G-XXXXXXXXXX

POSTAL_SERVICE_HELPER_WEBSITE_URL='https://domain.tld.cc/'
POSTAL_SERVICE_QUERY_URL='https://domain.tld.cc/api/zip?zip='

PRIVATE_API='https://api.domain.tld'
PUBLIC_API='https://api.domain.tld/public-api'

# API request timeout in milliseconds (default: 3000ms = 3s)
# Lower values fail faster and prevent CPU limit issues, but may cause timeouts on slow networks
API_TIMEOUT=3000

# Edge Cache Configuration (Cloudflare Workers KV)
# Duration in seconds for caching SSR-rendered HTML at Cloudflare's edge (default: 30s)
# Set to 0 to disable edge caching
EDGE_CACHE_DURATION=30

QUERY_STRING_SPECIAL_PARAMETERS_AMOUNT='amount'
QUERY_STRING_SPECIAL_PARAMETERS_REFERRER='ref'

RECEIPTS_BASE='https://receipts.domain.tld'

# Sentry Error Tracking Configuration
# Get these values from https://sentry.io/settings/your-org/projects/your-project/keys/
SENTRY_AUTH_TOKEN='sntrys_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx'  # Auth token for sourcemap uploads
SENTRY_DSN_PUBLIC='https://xxxxxxxxxxxxxxxxxxxxxxxxxxxxx@oxxxxx.ingest.us.sentry.io/xxxxxxxxxxxxxxx'  # Public DSN for error reporting
SENTRY_ORG='your-org'  # Sentry organization slug
SENTRY_PROJECT='your-project'  # Sentry project slug
SENTRY_TRACES_SAMPLE_RATE='0.1'  # Percentage of transactions to trace (0.0 to 1.0)
SENTRY_REPLAY_SAMPLE_RATE='0'  # Percentage of normal sessions to replay (0.0 to 1.0)
SENTRY_ERROR_REPLAY_SAMPLE_RATE='1.0'  # Percentage of error sessions to replay (0.0 to 1.0)

URL_OF_PRIVACY_POLICY='https://domain.tld.cc/pp'
URL_OF_USE_TERMS='https://domain.tld.cc/tos'
